// import { Model, model, Schema, SchemaDefinition } from 'mongoose';
// import { IGame } from './game';

// const schemaDefinition: SchemaDefinition = {
//   id: String,
//   round: Number,
//   rowStatus: [{ type: Boolean }],
//   rowMessages: [{ type: String }],
//   tableValue: Number,
//   multiplier: Number,
//   drawnCards: [{ type: [{ type: Number }] }],
//   deck: [{ type: Number }],
// };

// export const gameSchema: Schema = new Schema(schemaDefinition);

// export const game: Model<IGame> = model<IGame>('Game', gameSchema);

// export default game;
